# Database (PostgreSQL 16+ with pgvector)
# If using Docker: docker compose up -d  then  npm run db:setup
# Use postgres:postgres for Docker; replace with your credentials for existing DB
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_knowledge_base

# JWT — generate a secure random string for production (e.g. openssl rand -hex 32)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# AI Provider: use Ollama (llama3.2) or Gemini
# Option A — Ollama (local, no API key): run `ollama pull llama3.2` first
AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
# OLLAMA_TIMEOUT_MS=120000
# Option B — Google Gemini (cloud)
# GEMINI_API_KEY=

# YouTube Data API (learning course videos from summarized topics)
# Enable "YouTube Data API v3" at https://console.cloud.google.com/apis/library
# Create credentials → API key. Leave empty if not needed.
YOUTUBE_API_KEY=

# Server
PORT=3000
NODE_ENV=development
